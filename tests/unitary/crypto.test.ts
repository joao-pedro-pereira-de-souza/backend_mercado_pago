import { Response, Request, NextFunction } from 'express';
import { describe, it, expect, jest, beforeEach } from '@jest/globals';


describe('#Unitary: libs/crypto', () => {
    it.todo('should contain all parameters and return the hash of the input value');
    it.todo('should return error when sending invalid compare value');
    it.todo('should return success compare value');
});
